# Prompt Presets Package

This package will encapsulate the default culling styles, marketplace prompt schema, and seed data.

It will export:
- Type-safe prompt definitions shared between apps
- Utilities for score aggregation (AI and human ratings)
- Migration helpers for syncing presets to the backend
